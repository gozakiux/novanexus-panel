# Claves SECRETAS — SOLO para scripts locales/servidor (carpeta tools/, gitignored).
# NUNCA con prefijo VITE_ (eso las metería en el bundle público del cliente).
# NUNCA versionar valores reales. Guárdalas en tu .env.local junto a las públicas.

SUPABASE_URL=https://TU-PROYECTO.supabase.co
SUPABASE_SECRET_KEY=sb_secret_xxxxxxxx
SUPABASE_SERVICE_ROLE_KEY=eyJhbG...   # JWT service_role (salta RLS)
